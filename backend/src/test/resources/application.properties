spring.application.name=backend-test
# Test classpath only: production still requires an explicit HTTPS Auth0 issuer.
mulino.auth.issuer=https://mulino-auth-test.example/
mulino.auth.audience=urn:mulino:erp-api
mulino.auth.jwks-uri=https://mulino-auth-test.example/.well-known/jwks.json
mulino.auth.worker-client-id=test-worker
# Many cached test contexts otherwise each reserve ten idle connections against PostgreSQL's
# default 100-client limit. Four connections still cover the two-worker concurrency fixtures.
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=0
